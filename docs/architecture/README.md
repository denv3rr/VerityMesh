# Architecture

Architecture documents should explain the run lifecycle, service boundaries,
data model, transaction boundaries, artifact flow, telemetry, and release gate.

Initial required diagrams:

- system context;
- run lifecycle;
- data/provenance model;
- worker queue and retry flow;
- trust boundaries.

Do not add architecture diagrams that imply services or scale the project has
not implemented or measured.

