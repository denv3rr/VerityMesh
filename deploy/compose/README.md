# Docker Compose Deployment

Owns the local development and first deployable Docker Compose setup.

Future responsibilities:

- API service;
- worker service;
- web service;
- PostgreSQL;
- Redis;
- local S3-compatible object storage if artifacts require it;
- telemetry dependencies when introduced.

Do not add production orchestration before measured need exists.

