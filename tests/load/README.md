# Load Tests

Load tests should measure throughput, queue wait, p50/p95/p99 latency, error
rate, database hot spots, worker recovery, and provider-failure behavior under a
documented load profile.

Never generalize beyond the measured environment.

