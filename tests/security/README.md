# Security Tests

Security tests should cover untrusted input handling, prompt injection, poisoned
documents, unauthorized tool use, exfiltration attempts, malformed evaluator
output, secret redaction, safe report rendering, authz boundaries, and budget
limits.

