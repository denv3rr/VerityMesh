# Unit Tests

Unit tests cover strict contract validation, deterministic scorers, the fake
support workflow, result bundle generation, and CLI entry-point behavior.

They must remain offline and must not require hosted model credentials.
