# GitHub Workflows

Future CI should run formatting, linting, type checking, unit tests, migration
validation, integration tests, security checks, schema drift checks, and the
offline deterministic end-to-end path.

Hosted-model tests must be opt-in and budget-capped.

